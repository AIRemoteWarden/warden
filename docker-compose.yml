services:
  server:
    build:
      context: ./server
    image: ai-warden-server:local
    environment:
      WARDEN_CONTROL_ADDR: ":8080"
      WARDEN_PUBLIC_HOST: "localhost"
    ports:
      - "8080:8080"
